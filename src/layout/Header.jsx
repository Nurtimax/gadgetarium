import { AppBar, Container, Toolbar } from "@mui/material";
import React from "react";

const Header = () => {
  return (
    <AppBar>
      <Container>
        <Toolbar></Toolbar>
      </Container>
    </AppBar>
  );
};

export default Header;
