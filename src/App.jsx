// import React from "react";
// import ProductCard from "./components/UI/card/ProductCard";
// import { Container } from "@mui/material";
// import { product } from "./assets/images";
// import Button from "./components/UI/button/Button";
const App = () => {
  return (
    <>
      {/* <Container style={{ display: "flex" }}>
        {product.map((item, id) => (
          <ProductCard key={id} {...item} sort="LIKE" />
        ))}
      </Container>
      <Button variant="outlined">Еще показать</Button>
      <Container style={{ display: "flex" }}>
        {product.map((item, id) => (
          <ProductCard key={id} {...item} sort="NEW" />
        ))}
      </Container>
      <Container style={{ display: "flex", gap: 5 }}>
        {product.map((item, id) => (
          <ProductCard key={id} {...item} sort="DISCOUNT" />
        ))}
      </Container> */}
    </>
  );
};

export default App;
